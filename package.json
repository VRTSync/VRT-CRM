{
  "name": "vrtsync-crm",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd client && pnpm build",
    "start": "NODE_ENV=production node server/index.js",
    "db:push": "pnpm --dir server exec drizzle-kit push --config=../drizzle.config.js",
    "db:seed": "node server/db/seed.js"
  }
}
